export interface ResCreateUserI {
    /**
     * Message to display to the user when request its done
     */
    message: string;

    /**
     * Http status according the query insertion
     */
    httpStatus: number;
}